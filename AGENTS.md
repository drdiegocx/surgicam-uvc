# Repository Guidelines

These instructions apply to the entire repository.

- Use Node.js CommonJS (`require`) modules unless existing files demonstrate otherwise.
- Keep the frontend fully responsive and avoid introducing scrollbars on the primary viewer layout.
- Update the README whenever you make significant changes to setup, controls, or runtime behavior.
- Place captured or generated media assets inside the `media/` directory and avoid committing large binaries.
